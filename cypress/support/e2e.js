// Import commands.js using ES2015 syntax:
import "./commands";

// Alternatively you can use CommonJS syntax:
// require('./commands.js')

// Add global beforeEach hook
beforeEach(() => {
  // cy.log('Running before each test in support file');
});

// Add global afterEach hook
afterEach(() => {
  // cy.log('Running after each test in support file');
});
